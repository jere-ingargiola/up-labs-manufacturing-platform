{
  "name": "get-equipment-metrics-lambda",
  "version": "1.0.0",
  "description": "Lambda function for retrieving equipment metrics",
  "main": "handler.js",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "deploy": "npm run build && zip -r function.zip dist/ node_modules/",
    "clean": "rm -rf dist/",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/node": "^20.9.0",
    "@types/pg": "^8.10.7",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.8",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}