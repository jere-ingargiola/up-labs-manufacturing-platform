{
  "name": "ultra-fast-alerts-lambda",
  "version": "1.0.0",
  "description": "Ultra-low latency alert processing Lambda (<100ms)",
  "main": "handler.js",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "deploy": "npm run build && zip -r function.zip dist/ node_modules/",
    "clean": "rm -rf dist/",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@aws-sdk/client-sns": "^3.450.0",
    "@aws-sdk/client-eventbridge": "^3.450.0"
  },
  "devDependencies": {
    "@types/node": "^20.9.0",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.8",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}